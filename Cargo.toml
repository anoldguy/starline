[package]
name = "starline"
version = "0.1.0"
edition = "2024"
description = "A fast Rust status line for Claude Code"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
gix = { version = "0.80", default-features = false, features = ["status"] }
